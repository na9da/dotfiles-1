[user]
    name = Phil Hagelberg
    email = technomancy@gmail.com
[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
[push]
    default = tracking
[diff "clojure"]
    xfuncname = "^\\(.*"
[alias]
    shorten = "!sh -c 'curl -i http://git.io -F url=$1' -"
[heroku]
	remote = heroku
[github]
	user = technomancy
	oauth-token = !gpg --batch -q -d .chorts/github.gpg 2> /dev/null
