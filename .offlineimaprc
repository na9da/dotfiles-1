[general]
ui = ttyui
accounts = Personal
pythonfile = ~/.offlineimap.py

# Personal

[Account Personal]
localrepository = LocalPersonal
remoterepository = Personal
# autorefresh = 5

[Repository LocalPersonal]
type = Maildir
localfolders = ~/mail/

[Repository Personal]
type = IMAP
remotehost = mail.hagelb.org
remoteuser = phil@hagelb.org
remotepasseval = get_authinfo_password("mail.hagelb.org", "phil@hagelb.org")
ssl = yes
holdconnectionopen = true
keepalive = 60
folderfilter = lambda folder: folder in ['INBOX']
