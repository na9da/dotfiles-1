[general]
ui = TTY.TTYUI
accounts = Gmail

[Account Gmail]
autorefresh = 30
localrepository = Local
remoterepository = Remote
# quick = 6

[Repository Local]
type = IMAP
preauthtunnel = MAIL=maildir:$HOME/Maildir /usr/lib/dovecot/imap
holdconnectionopen = yes

[Repository Remote]
type = IMAP
remotehost = imap.gmail.com
remoteuser = phil@hagelb.org
remotepassfile = ~/.gmailpass
ssl = yes
# nametrans = lambda foldername: re.sub ('^\[gmail\]', 'gmail',
#                                re.sub ('sent_mail', 'sent',
#                                re.sub ('starred', 'flagged',
#                                re.sub (' ', '_', foldername.lower()))))
folderfilter = lambda foldername: foldername not in [ '[Gmail]/All Mail', '[Gmail]/Spam' ]
