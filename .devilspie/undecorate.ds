(if (or (matches (window_name) "Firefox")
        (matches (window_name) "Minefield")
        (matches (application_name) "macs")
        (matches (application_name) "conkeror")
        (matches (application_name) "erminal")
        (matches (application_name) "rxvt")
        (matches (application_name) "irc")
        (matches (application_name) "uzbl")
        (matches (application_name) "hromium"))
    (begin
     (undecorate)
     (maximize)))
