#!/bin/bash

HUB_CONFIG_GPG=$HOME/.config/hub.toml.gpg
export HUB_CONFIG=$(mktemp -t tmp.XXXXXXXX.hub)

function finish {
  rm -rf "$HUB_CONFIG"
}

trap finish EXIT

gpg -q -d "${HUB_CONFIG_GPG}" > "${HUB_CONFIG}"

exec "${HOME}/src/hub/bin/hub" $@
