#!/bin/sh

TARGET="/media/backup"

rsync -r $HOME/music/ "$TARGET"/music/
rsync -r $HOME/documents/ "$TARGET"/documents/
rsync -r $HOME/src/ "$TARGET"/src/
rsync -r $HOME/Photos/ "$TARGET"/Photos/

# TODO: shotwell db
